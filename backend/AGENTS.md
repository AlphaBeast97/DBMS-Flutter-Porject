# Backend Agent Notes

- Keep business logic in the database via stored procedures/functions.
- Backend only validates inputs, calls stored routines, and formats responses.
- Keep routes modular and avoid direct table access from code.
- Prefer clear request/response shapes for presentation demos.
